services:
  # react-prod:
  #   build:
  #     context: .
  #     dockerfile: .docker/prod/Dockerfile
  #   image: docker-reactjs-sample
  #   ports:
  #     - "8080:8080"

  react-dev:
    build:
      context: .
      dockerfile: .docker/dev/Dockerfile
    ports:
      - "3000:3000"
    develop:
      watch:
        - action: sync
          path: .
          target: /app
