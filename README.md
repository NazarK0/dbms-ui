# Database Management System App

## Development

* To run project in development mode run ```docker compose up --watch --build --force-recreate --remove-orphans``` or ```make dev```(Linux)
* To stop project run ```docker compose down```